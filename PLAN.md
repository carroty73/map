"등록하기"버튼의 진한 오렌지색과 같이 만들어줘.
텍스트 필드는 하얀색
검색, 길찾기, 즐겨찾기 글씨는 하얀색

## 결정: 회사 DB / 핫플 DB는 합치지 않는다

- 노션에 "회사 소개" DB와 "핫플(맛집) 소개" DB가 별도로 존재.
- 두 DB를 하나로 합치지 않고, 노션에서는 그대로 분리 유지.
- 앱(백엔드)에서 각각 조회해서, 지도 위에 마커 아이콘/카테고리 등으로
  구분해서 같이 보여주는 방식으로 진행한다.
- 이유: 두 DB가 담는 정보(속성)가 서로 달라 하나로 합치면 안 쓰는 필드가
  계속 비게 되고 관리가 지저분해짐.

## 계획: "검색" 탭 안에서 회사/맛집을 토글로 구분

- 탭 개수는 늘리지 않는다 (검색/즐겨찾기/등록/안내 4개 유지).
- "검색" 패널 상단에 "회사"/"맛집" 토글(또는 세그먼트 버튼)을 추가한다.
- 토글에 따라 그 아래 카테고리 아이콘 목록과 장소 목록이 바뀐다.
  - 맛집: 지금 있는 카테고리(먹을 거리/마실 거리/즐길 거리/숙소/볼 거리/
    알아갈 거리/쉴 거리/교통 등)
  - 회사: 회사 DB의 속성에 맞는 카테고리로 별도 정의 필요 (업종 등 확인 필요)
- 지도 위 마커도 회사/맛집을 아이콘이나 색으로 구분해서 표시한다.
- 지금은 UI(토글 버튼 + 카테고리 전환)만 만들고, 실제 두 DB 데이터를
  불러와 마커로 뿌리는 연동은 이후 Phase에서 진행한다.

### 확인 필요

- 회사 DB의 실제 속성(컬럼) 목록 — 카테고리를 뭘로 나눌지 결정하려면 필요.