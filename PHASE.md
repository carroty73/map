# PHASE.md

## Phase 0

간단한 html 파일을 만들어 FastAPI로 서비스해보는 실습. (세부 내용: `sandbox/test.txt`)

- test.html 작성 → .env에 서버 정보 관리 → FTP로 test.html/test.py 업로드
- test.py(FastAPI 미니 서버)로 8001 포트에서 `/test` 라우트 서비스
- 브라우저 접속 확인 완료 (로컬/외부 IP 모두 정상)

## Phase 1 (완료)

노션 DB를 읽는 것만 한다. 다른 작업은 하지 않는다.

- `sandbox/phase1_notion_read_cli.py`: 노션 DB 조회 후 터미널에 출력
- `sandbox/phase1_notion_read_web.py`: 같은 조회 로직을 FastAPI로 감싸 브라우저(`/`)에 출력
- 노션 통합 공유 문제 해결 후 로컬에서 정상 동작 확인 (터미널/브라우저 둘 다)

## Phase 2

목표: 노션 DB 조회를 캐싱해서 빠르게 만들고, Flask(`app.py`)를 FastAPI로 옮긴다.

- 노션 DB(약 1,000개 행) 조회 결과를 캐싱한다 — 서버 시작 시 1회 조회 후
  메모리에 저장, 매 요청마다 재조회하지 않도록.
- `app.py`의 라우트(`/api/places`, `/company/`, `/count.txt`, `/api/boundaries` 등)를
  FastAPI로 하나씩 옮긴다.
- Flask 전용 문법(`jsonify`, `send_file` 등)을 FastAPI 대응 방식으로 교체.
- `requirements.txt` 작성.
- FastAPI 버전을 서버에 배포 후 `/company/`가 기존과 동일하게 동작하는지 확인.
- 기존 Flask `app.py`는 확인 전까지 서버에 유지 (롤백 대비).

보류: 노션 DB 실시간 반영(Webhook, `last_edited_time` 기반 부분 갱신)은 지금은
하지 않는다 — 업체 수가 아직 많지 않아(1,000개 미만) "전체 재조회 + 캐싱"만으로
충분. 참고로 방식만 남겨둠:
- 폴링: 주기적으로 재조회. 구현 간단, 반영까지 지연 있음.
- Webhook: 변경 시 노션이 알려줌. 실시간에 가깝지만 수신용 라우트와 노션 쪽
  URL 등록/검증 절차 필요.

확인 필요: 서버에서 FastAPI 앱을 상시 실행할 방법 (터미널 직접 실행 vs
systemd 서비스 등록).

## Phase 3 (완료)

목표: 지도 표시하는 UI만 만들기 (백엔드 연동은 이후 Phase).

- 기존 `index.html`은 `backup/index.html`로 백업하고, 카카오맵 스타일
  레이아웃(좌측 사이드바 + 우측 지도)으로 새로 제작.
- 사이드바: 검색 / 즐겨찾기 / 등록 / 내 정보 4개 탭, 클릭 시 애니메이션
  없이 즉시 전환. 우측 하단 화살표 버튼으로 사이드바 전체 접기/펼치기
  (접으면 카테고리 아이콘만 남는 좁은 형태).
- "검색" 탭 안에 회사/맛집 토글 추가 — 아래 카테고리 아이콘 세트만 전환
  (회사: 노션 회사 DB의 `카테고리` 속성 값 기준, 맛집: 기존 카테고리).
  둘 다 더미 장소 목록으로 자리만 채움.
- "등록" 탭: 원래 별도 페이지였던 `input.html`(장소 등록 폼)을 단일 페이지
  방향에 맞춰 이 탭 안으로 통합. 이후 `input.html`/`input.css`/
  `/input/` 라우트는 삭제.
- "즐겨찾기", "내 정보" 탭도 더미 데이터로 채움.
- 지도 영역: 실제 Leaflet 지도 삽입(마커/검색 연동은 아직 없음), 커스텀
  확대/축소 버튼 연결. Leaflet 기본 attribution 컨트롤과 위치가 겹치던
  문제 수정.
- 사이드바 상단(로고/검색창/탭)과 등록 폼 버튼 등에 오렌지 톤 배색 적용,
  `intro.html`도 같은 톤으로 통일.
- 사이드바 하단에 광고 카드 / 공지 링크 / 고객센터·개인정보처리방침 링크
  추가 (노션 페이지로 연결).
- 회사 DB(`NOTION_COMPANY_DB_ID`)와 핫플 DB는 노션에서 합치지 않고
  분리 유지하기로 결정 (세부 내용: `PLAN.md`).
- 아이콘: "맛집" 카테고리는 emoji 대신 `static/ico/pack_2/` 실제 이미지로 교체.
- 모바일 반응형 추가: 768px 이하에서 사이드바가 상단 바로 전환(높이 55vh
  제한), 지도가 나머지 공간 차지. 상단 바 하단 가운데에 `▲`/`▼` 버튼을
  추가해 카테고리/목록/폼 부분만 접었다 펼 수 있게 함 (PC용 좌우 접기
  토글은 모바일에서 숨김).
- 즐겨찾기/등록/내 정보 패널에서 탭 이름과 중복되던 패널 내부 제목 줄 제거,
  등록 폼/내 정보 상단 여백 조정.
- 반응형 작업 전 `backup/phase3_desktop/`에 당시 `index.html`,
  `company_v2.css` 백업해둠.

## Phase 4 (완료)

목표: Phase 3에서 모양만 만들어둔 UI에 실제 데이터를 연결하고, 지도 페이지의
기능을 완성한다. (배포/운영 관련 작업은 Phase 5로 분리.)

- `/api/places`가 회사 DB(`NOTION_COMPANY_DB_ID`)와 핫플 DB(`NOTION_HOT_DB_ID`)
  둘 다 조회해서 각 장소에 `domain`("company"/"hotspot") 필드를 붙여 한 번에
  반환하도록 수정.
- 버그 수정: 기존에는 회사 데이터를 가져오면서 `CATEGORY_VISIBLE`이 핫플
  카테고리 이름으로만 정의돼 있어서, 회사 장소가 전부 "기타"로 뭉뚱그려지던
  문제를 발견하고 도메인별 `CATEGORY_VISIBLE` 구조로 분리.
- `index.html`의 회사/맛집 토글과 카테고리 아이콘 버튼이 실제로 지도 위
  마커를 켜고 끄도록 연결. 회사 카테고리는 새 아이콘 이미지 없이 기존 토글
  버튼의 이모지를 재사용(`L.divIcon`)해서 마커로 표시.
- 회사 소개 페이지(장소 상세 페이지) 디자인 확인용 정적 샘플 3개를
  `static/samples/`에 추가 (데이터 미연동, 레이아웃/톤 확인용).
- 검색 기능 구현: 사이드바 검색창에 입력하면 현재 도메인(회사/맛집) 안에서
  이름 → 주소 순으로 매칭되는 장소를 "장소 목록" 영역에 표시. 클릭하면
  해당 마커로 `flyTo` + 팝업 열기 + 깜빡임 효과. 카테고리 영역과 장소 목록
  사이 구분선 추가, 더미 placeholder 데이터 제거(기본 안내 문구로 대체).
- `boundaries/`에 전국 17개 시/도 행정동 경계 geojson 파일을 받아 정리
  (인구 통계 등 무관한 파일은 제거). 서버 측 데이터라 git에는 올리지 않고
  `.gitignore`에 추가.
- 지도 우측 상단에 Leaflet 레이어 컨트롤을 추가해 경계선을 지역별로
  켜고 끌 수 있게 연결. 17개를 한 번에 다 그리면 무거워서, 체크할 때만
  해당 geojson을 내려받는 지연 로딩 방식으로 구현. 기본값은 서울만 켜짐.
- `_boundary_display_name`에서 파일명의 "hangjeongdong" 접두어도 제거하도록
  수정 (표시 이름이 "hangjeongdong 서울특별시 경계"처럼 나오던 문제).
- 같은 레이어 컨트롤에 지도 배경(밝은 지도/어두운 지도/기본 지도) 라디오
  버튼도 추가 — 버튼 하나 눌러서 펼치면 배경 전환 + 경계선 토글이 한 곳에
  모여 있음.
- 마커 팝업과 검색 결과 각 항목에 "자세히 보기"(해당 장소 Notion 페이지로
  이동) 링크 추가.
- 즐겨찾기 기능: 마커 팝업 / 검색 결과 / 즐겨찾기 탭 어디서든 ★ 버튼으로
  추가·해제. 서버가 아니라 브라우저 `localStorage`에 저장(로그인 기능이
  없어서 계정 서버 저장 대신 선택). 장소 식별은 Notion에 별도 ID가 없어서
  `도메인|이름|주소` 조합으로 처리.
- 등록 기능: 등록 폼에 검색 탭과 별개인 회사/맛집 토글 추가, 카테고리
  드롭다운은 선택한 도메인에 맞춰 자동 구성. `POST /api/register`가 주소를
  Kakao로 지오코딩한 뒤 해당 Notion DB에 새 페이지를 생성하고, 성공하면
  새로고침 없이 지도에 마커를 바로 추가.
- 버그 수정: `.env`의 카카오 키 변수명(`kAKAO_REST_KEY`)과 `app.py`가 읽던
  이름(`KAKAO_API_KEY`)이 달라서 지오코딩이 계속 실패하던 문제 발견·수정.
  기존 마커들은 Notion에 좌표가 캐싱돼 있어서 이 버그가 안 드러났었음 —
  등록 기능(라이브 지오코딩 필요)을 붙이면서 처음 발견됨.

미완료:
- 사이드바 "내 정보" 탭은 아직 더미 데이터 그대로.

## Phase 5 (진행 중)

목표: 완성된 앱을 실제 도메인으로 배포하고, 운영에 필요한 기반(조회수 등)을
갖춘다.

- GitHub 저장소 이름을 `company` → `map`으로 변경, 로컬 `git remote` URL도
  맞춰서 갱신.
- Flask 라우트 이름 변경: `/company/` → `/map` → `/main`. 처음엔 저장소
  이름에 맞춰 `/map`으로 바꿨는데, `map.carroty.com/map`처럼 "map"이
  주소에 두 번 나와 헷갈린다는 이유로 `/main`으로 다시 변경. `intro.js`
  리다이렉트, 샘플 페이지 링크, `CLAUDE.md`/`README.txt`도 같이 갱신.
- 도메인 연결: `https://map.carroty.com`이 Cloudflare 경유로 정상 서비스되는
  것 확인. `intro.html`의 OG/트위터 메타 태그와 화면에 보이던 도메인 텍스트를
  예전 IP(`221.138.105.134:8001`)에서 새 도메인으로 갱신.
- 배포 후 브라우저가 예전 `intro.js`(리다이렉트 대상이 `/map`이던 버전)를
  캐싱해서 계속 옛 경로로 이동하던 문제 발생 — `intro.js`에 캐시버스팅
  쿼리스트링(`?v=2`)을 붙여서 해결.
- `.env`에 SSH/Telnet 접속 정보 항목 추가, SSH로 서버(`192.168.1.3`) 접속
  확인(Python 3.10.12). FTP로 `webapps/map`에 앱 전체
  (`app.py`/`index.html`/`static/`/`templates/`) 업로드 후 `nohup`으로
  8001 포트에 백그라운드 실행. `boundaries/`의 geojson 17개도 기본 배포
  대상에서 빠져있어 경계선이 서버에서 안 보였던 것을 확인하고 별도 업로드.
- 서버 `.env`는 앱 실행에 실제 필요한 값(Notion 토큰/DB ID, Kakao 키,
  `MY_IP`)만 최소로 구성 — FTP/SSH/GitHub 같은 로컬 배포용 자격증명은
  서버에 둘 필요가 없어서 제외. 파일 권한도 소유자 전용(600)으로 제한.
- Notion 회사 DB 카테고리 스키마 변경 반영: "서버SW" 추가, "기구 / 케이스"
  삭제, "PCB 아트웍" + "PCB 제작"을 "PCB 아트웍/제작"으로 병합.
  `CATEGORY_VISIBLE`, 카테고리 버튼/아이콘/등록 폼 드롭다운 동기화.
- 조회수 기능: 기존 `/count.txt`(정수 하나, 실제로는 어디서도 호출 안 되던
  죽은 라우트)를 `/api/view-count` + `view_count.json`으로 교체해 일별/누적
  카운트를 관리(`{"total": N, "daily": {"YYYY-MM-DD": N}}`, 90일 지난 날짜는
  저장할 때 자동 정리). 사이드바 헤더의 더미 숫자("Today 128" 등)를 실제
  값으로 교체.
- 소유자 IP 제외 로직을 하드코딩된 상수(`MY_IP`)에서 `.env` 값으로 이동.
  공인 IP만 등록돼 있어서 집 LAN에서 직접 접속하면 카운트가 올라가던 문제를
  발견하고, 콤마로 여러 IP(공인 IP + LAN IP)를 등록할 수 있게 개선.

미완료 / 확인 필요:
- 사이드바 "내 정보" 탭 더미 데이터.
- Phase 2에서 계획했던 Flask → FastAPI 전환.
- 상시 실행 관리: 지금은 `nohup`으로만 띄운 상태 — systemd 서비스 등록 등
  프로세스가 죽었을 때 자동 재시작되는 방식은 아직 없음.

## Phase 6 (완료)

목표: 지도 페이지 접속이 발생하면 알림을 받는다.

카카오톡 메시지 API는 OAuth 로그인 + 토큰 갱신 로직이 필요해서 번거로움 —
대신 [ntfy.sh](https://ntfy.sh)로 결정. 가입/앱 등록/토큰 발급이 전혀 없이,
정해둔 토픽 이름으로 HTTP POST 한 번이면 끝이라 훨씬 간단함.

- `.env`에 `NTFY_TOPIC`(추측 어렵게 랜덤 문자열 포함) 추가.
- `app.py`에 `notify_new_visit()` 추가: `/api/view-count`가 방문자 카운트를
  올릴 때(=소유자 IP가 아닐 때, `MY_IPS` 제외 로직 재사용) `https://ntfy.sh/<토픽>`으로
  "오늘 N번째 방문 (누적 M)" 메시지를 POST. 실패해도 `try/except`로 페이지
  응답에는 영향 없게 처리.
- 받는 쪽은 ntfy 앱(iOS/Android) 설치 후 같은 토픽 구독하면 푸시 알림 도착.
- 로컬에서 실제로 ntfy.sh에 메시지가 도착하는 것까지 확인 완료.
- 사용자가 실제로 ntfy 앱을 설치하고 토픽을 구독함 — 서버 배포 후 실제
  방문 알림이 폰으로 오는지까지 확인 필요.
